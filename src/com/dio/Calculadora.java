package com.dio;

import static java.lang.System.*;

public class Calculadora {
    public static void soma(double a, double b ) {
        out.println("A soma de " + a + " com " + b + " é: " + (a + b));
    }
}
